// @flow
'use strict';

import { Component } from 'react';

const START_LOADING = 'START_LOADING';
const STOP_LOADING = 'STOP_LOADING';
const SET_NETWORK_ENABLED = 'SET_NETWORK_ENABLED';

const SHOULD_MOCK = false;
const MOCKNETWORKENABLED = true;

export {
  START_LOADING,
  STOP_LOADING,
  SET_NETWORK_ENABLED,
  SHOULD_MOCK,
  MOCKNETWORKENABLED,
};
